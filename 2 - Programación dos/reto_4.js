'use strict';

const moneyPerPerson = (128 - 2) / 9
const moneyAna = moneyPerPerson + 2

console.log('Cada persona debe pagar un total de ' + moneyPerPerson + ' euros. Ana debe pagar ' + moneyAna + '.');
