let firstOperation = 2 + 3 + '5' //Se realiza la suma de los primeros dos números pero luego ese resultado se concatena con el string.
let secondOperation = '2' + 3 + 5 //Al haber un string al inicio, los números son convertidos a strings y se concatenan.
